name: shop_management
description: Ứng dụng quản lý bán hàng với Supabase
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  supabase_flutter: ^2.16.0
  flutter_dotenv: ^5.1.0
  provider: ^6.1.2
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  image_picker: ^1.0.7
  fl_chart: ^0.66.2
  dropdown_search: ^5.0.6
  uuid: ^4.3.3
  
  # Web-specific
  url_strategy: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
